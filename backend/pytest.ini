[pytest]
testpaths = tests
addopts = -ra -q --strict-markers
asyncio_mode = auto
markers =
    unit: fast and isolated unit tests
    integration: db or service-backed integration tests
    e2e: full end-to-end tests
    slow: long-running tests
